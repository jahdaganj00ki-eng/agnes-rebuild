package com.agnes.bundle_agnes.ui.chat.model;
//
// Agnes Rebuild Spec — placeholder file (see README.md & docs/ for the rebuild specification).
// This file intentionally contains no code from the original app (proprietary).
// It marks the position and responsibility of `ImChatReloadDiskPayload` inside module `com.agnes.bundle_agnes.ui.chat.model` so that
// structure-analysis tools (e.g. the GitReverse method) can reconstruct the app's architecture.
//
public final class ImChatReloadDiskPayload {
    // Implements responsibilities described for ImChatReloadDiskPayload in docs/.
}
