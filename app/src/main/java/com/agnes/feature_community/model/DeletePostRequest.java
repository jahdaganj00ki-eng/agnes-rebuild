package com.agnes.feature_community.model;
//
// Agnes Rebuild Spec — placeholder file (see README.md & docs/ for the rebuild specification).
// This file intentionally contains no code from the original app (proprietary).
// It marks the position and responsibility of `DeletePostRequest` inside module `com.agnes.feature_community.model` so that
// structure-analysis tools (e.g. the GitReverse method) can reconstruct the app's architecture.
//
public final class DeletePostRequest {
    // Implements responsibilities described for DeletePostRequest in docs/.
}
