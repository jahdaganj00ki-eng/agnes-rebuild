package com.sobrr.agnes.data.model.gson;
//
// Agnes Rebuild Spec — placeholder file (see README.md & docs/ for the rebuild specification).
// This file intentionally contains no code from the original app (proprietary).
// It marks the position and responsibility of `StringOrNumberTypeAdapter` inside module `com.sobrr.agnes.data.model.gson` so that
// structure-analysis tools (e.g. the GitReverse method) can reconstruct the app's architecture.
//
public final class StringOrNumberTypeAdapter {
    // Implements responsibilities described for StringOrNumberTypeAdapter in docs/.
}
