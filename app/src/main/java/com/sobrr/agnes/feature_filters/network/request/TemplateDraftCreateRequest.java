package com.sobrr.agnes.feature_filters.network.request;
//
// Agnes Rebuild Spec — placeholder file (see README.md & docs/ for the rebuild specification).
// This file intentionally contains no code from the original app (proprietary).
// It marks the position and responsibility of `TemplateDraftCreateRequest` inside module `com.sobrr.agnes.feature_filters.network.request` so that
// structure-analysis tools (e.g. the GitReverse method) can reconstruct the app's architecture.
//
public final class TemplateDraftCreateRequest {
    // Implements responsibilities described for TemplateDraftCreateRequest in docs/.
}
