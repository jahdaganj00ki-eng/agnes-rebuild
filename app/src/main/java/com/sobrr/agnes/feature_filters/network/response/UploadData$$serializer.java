package com.sobrr.agnes.feature_filters.network.response;
//
// Agnes Rebuild Spec — placeholder file (see README.md & docs/ for the rebuild specification).
// This file intentionally contains no code from the original app (proprietary).
// It marks the position and responsibility of `UploadData$$serializer` inside module `com.sobrr.agnes.feature_filters.network.response` so that
// structure-analysis tools (e.g. the GitReverse method) can reconstruct the app's architecture.
//
public final class UploadData__serializer {
    // Implements responsibilities described for UploadData$$serializer in docs/.
}
